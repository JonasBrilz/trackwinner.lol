steps:
  - name: gcr.io/cloud-builders/docker
    dir: frontend
    args:
      - build
      - -t
      - europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/bighack-berlin
      - .

  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/bighack-berlin

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    args:
      - gcloud
      - run
      - deploy
      - bighack-berlin
      - --image=europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/bighack-berlin
      - --region=europe-west1
      - --allow-unauthenticated

options:
  logging: CLOUD_LOGGING_ONLY
